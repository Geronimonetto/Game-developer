Módulo 1: Comandos iniciais
Pergunta: leia uma url utilizando numpy, essa API tem 87 colunas e o delimitador , a variável para os dados é dados?
A) dados = np.loadtxt(url, delimiter =',', usecols= np.arange(1,88))
B) dados = np.loadtxt(url, delimiter =',', usecols= np.arange(1,87))
C) dados = np.loadtxt(url, delimiter =',', usecols= np.arange(1,89))
D) dados = np.loadtxt(url, delimiter =';', usecols= np.arange(1,88))
Resposta Correta: dados = np.loadtxt(url, delimiter = ',', usecols = np.arange(1,88))

Pergunta: transforme a lista = [1,2,3,4] em um array do numpy?
A) dados = np.arange(10)
B) dados = np.numpy(lista)
C) np.array(lista)
D) *
Resposta Correta: np.array(lista)

Pergunta: Qual comando retorna o número de dimensões do array dados?  
A) dados.size  
B) dados.ndim  
C) dados.shape  
D) *  
Resposta Correta: dados.ndim

Pergunta: Qual comando retorna o número total de elementos no array dados?  
A) dados.ndim  
B) dados.shape  
C) dados.size  
D) *  
Resposta Correta: dados.size

Pergunta: Qual comando retorna as dimensões do array dados (número de linhas e colunas)?  
A) dados.ndim  
B) dados.size  
C) dados.shape  
D) *  
Resposta Correta: dados.shape

Pergunta: Qual comando retorna a transposição do array dados?  
A) dados.T  
B) dados.shape  
C) dados.size  
D) *  
Resposta Correta: dados.T

Pergunta: Dado que dados_transposto é a transposição de dados, qual comando retorna a primeira coluna de dados_transposto?  
A) dados_transposto[0, :]  
B) dados_transposto[:, 0]  
C) dados_transposto.T[:, 0]  
D) *  
Resposta Correta: dados_transposto[:, 0]


Pergunta: Qual comando plota os preços ao longo das datas para a primeira coluna (Moscou) dos dados precos?  
A) plt.plot(datas, precos[:, 0])  
B) plt.pyplot.plot(datas, precos)  
C) plt.pyplot.plot(datas, precos[:, 0])  
D) plt.plot(precos[:, 0], datas)  
Resposta Correta: plt.pyplot.plot(datas, precos[:, 0])

Pergunta: Qual comando extrai os preços para a cidade de Moscou a partir do array precos?  
A) moscou = precos[:, 0]  
B) moscou = precos[0, :]  
C) moscou = precos[0]  
D) moscou = precos[:, 1]  
Resposta Correta: moscou = precos[:, 0]

Pergunta: Qual comando verifica se os arrays moscou_ano3 e moscou_ano4 são exatamente iguais?  
A) np.array_equal(moscou_ano3, moscou_ano4)  
B) np.allclose(moscou_ano3, moscou_ano4)  
C) np.equal(moscou_ano3, moscou_ano4)  
D) np.isclose(moscou_ano3, moscou_ano4)  
Resposta Correta: np.array_equal(moscou_ano3, moscou_ano4)

Pergunta: Qual comando verifica se moscou_ano3 e moscou_ano4 são aproximadamente iguais, com uma tolerância padrão?  
A) np.array_equal(moscou_ano3, moscou_ano4)  
B) np.allclose(moscou_ano3, moscou_ano4)  
C) np.close(moscou_ano3, moscou_ano4)  
D) np.equal(moscou_ano3, moscou_ano4)  
Resposta Correta: np.allclose(moscou_ano3, moscou_ano4)

Pergunta: Qual comando plota os preços ao longo das datas para a cidade de Kaliningrado?  
A) plt.pyplot.plot(datas, kaliningard)  
B) plt.pyplot.plot(datas, moscou)  
C) plt.plot(kaliningard, datas)  
D) plt.plot(datas[:, 0], kaliningard)  
Resposta Correta: plt.pyplot.plot(datas, kaliningard)

Pergunta: Qual comando conta o número de valores NaN no array kaliningard?  
A) np.isnan(kaliningard)  
B) np.sum(isnan(kaliningard))  
C) sum(np.isnan(kaliningard))  
D) np.sum(kaliningard)  
Resposta Correta: sum(np.isnan(kaliningard))

Pergunta: Como podemos substituir o valor NaN no índice 4 do array kaliningard pela média dos elementos vizinhos, no índice 3 e 5?  
A) kaliningard[4] = mean([kaliningard[3], kaliningard[5]])  
B) kaliningard[4] = np.mean([kaliningard[3], kaliningard[5]])  
C) kaliningard[4] = kaliningard.mean([3, 5])  
D) kaliningard[4] = mean(kaliningard[3:5])  
Resposta Correta: kaliningard[4] = np.mean([kaliningard[3], kaliningard[5]])

Pergunta: Qual comando calcula a média dos valores do array moscou?  
A) np.mean(moscou)  
B) mean(moscou)  
C) np.mean([moscou])  
D) mean([moscou])  
Resposta Correta: np.mean([moscou])

Pergunta: Qual comando calcula a norma entre moscou e uma linha ajustada y definida como y = 0.52*x + 80?  
A) np.linalg.norm(moscou + y)  
B) np.norm(moscou - y)  
C) np.linalg.norm(moscou - y)  
D) norm(moscou - y)  
Resposta Correta: np.linalg.norm(moscou - y)

Pergunta: Qual comando cria um array de números inteiros de 0 a 9?  
A) np.array(10)  
B) np.arange(10)  
C) np.linspace(0, 10)  
D) np.range(10)  
Resposta Correta: np.arange(10)

Pergunta: Qual comando calcula a média dos elementos no array arr?  
A) arr.mean()  
B) np.mean(arr)  
C) arr.average()  
D) np.avg(arr)  
Resposta Correta: arr.mean()

Pergunta: Qual comando calcula o desvio padrão dos elementos no array arr?  
A) arr.std()  
B) np.std(arr)  
C) arr.deviation()  
D) np.deviation(arr)  
Resposta Correta: arr.std()

Pergunta: Qual comando calcula a soma de todos os elementos no array arr?  
A) arr.sum()  
B) np.sum(arr, axis=1)  
C) arr.total()  
D) np.total(arr)  
Resposta Correta: arr.sum()

Pergunta: Qual comando calcula a soma de cada coluna no array arr_aux?  
A) arr_aux.sum()  
B) arr_aux.sum(axis=0)  
C) arr_aux.sum(axis=1)  
D) arr_aux.sum(axis=2)  
Resposta Correta: arr_aux.sum(axis=0)

Pergunta: Qual comando calcula a soma de cada linha no array arr_aux?  
A) arr_aux.sum()  
B) arr_aux.sum(axis=0)  
C) arr_aux.sum(axis=1)  
D) arr_aux.sum(axis=2)  
Resposta Correta: arr_aux.sum(axis=1)

Pergunta: Qual comando retorna o array arr_aux com a soma acumulada dos elementos ao longo do eixo padrão?  
A) arr_aux.cumsum()  
B) arr_aux.sum()  
C) arr_aux.accumulate()  
D) np.cumsum(arr_aux, axis=1)  
Resposta Correta: arr_aux.cumsum()

Pergunta: Qual comando retorna o valor máximo no array arr_aux?  
A) arr_aux.max()  
B) arr_aux.maximum()  
C) np.maximum(arr_aux)  
D) np.max(arr_aux, axis=1)  
Resposta Correta: arr_aux.max()

Pergunta: Qual comando retorna o valor mínimo no array arr_aux?  
A) arr_aux.min()  
B) arr_aux.minimum()  
C) np.minimum(arr_aux)  
D) np.min(arr_aux, axis=1)  
Resposta Correta: arr_aux.min()

Pergunta: Qual comando retorna o índice do valor máximo no array arr_aux?  
A) arr_aux.indexmax()  
B) arr_aux.argmax()  
C) np.indexmax(arr_aux)  
D) np.argmax(arr_aux, axis=1)  
Resposta Correta: arr_aux.argmax()

Pergunta: Qual comando retorna o índice do valor mínimo no array arr_aux?  
A) arr_aux.indexmin()  
B) arr_aux.argmin()  
C) np.indexmin(arr_aux)  
D) np.argmin(arr_aux, axis=1)  
Resposta Correta: arr_aux.argmin()

Pergunta: Qual comando ordena os elementos do array arr_diff?  
A) arr_diff.sort()  
B) np.sort(arr_diff, axis=1)  
C) arr_diff.sorted()  
D) np.order(arr_diff)  
Resposta Correta: arr_diff.sort()

Pergunta: Qual comando retorna os valores únicos presentes no array arrayy?  
A) np.unique(arrayy)  
B) arrayy.unique()  
C) np.distinct(arrayy)  
D) np.uniq(arrayy)  
Resposta Correta: np.unique(arrayy)

Pergunta: Qual comando salva o array arr em um arquivo com o nome "some_array"?  
A) np.store("some_array", arr)  
B) np.save("some_array", arr)  
C) np.dump(arr, "some_array")  
D) save(np.array, "some_array")  
Resposta Correta: np.save("some_array", arr)

Pergunta: Qual função do NumPy gera números aleatórios de ponto flutuante dentro de um intervalo especificado?  
A) np.random.uniform()  
B) np.random.rand()  
C) np.random.array()  
D) np.random.randint()  
Resposta Correta: np.random.uniform()

Pergunta: Qual comando cria um array vazio em NumPy?  
A) np.random.array()  
B) np.zeros()  
C) np.array([])  
D) np.empty()  
Resposta Correta: np.array([])

Pergunta: Qual função do NumPy é usada para fixar a "semente" (seed) para a geração de números aleatórios, tornando-os reproduzíveis?  
A) np.seed()  
B) np.random.seed()  
C) np.random.uniform()  
D) np.fix_seed()  
Resposta Correta: np.random.seed()

Pergunta: Qual comando pode ser usado para garantir que a sequência de números gerados seja sempre a mesma em diferentes execuções?  
A) np.random.seed()  
B) np.random.array()  
C) np.fix_seed()  
D) np.seed()  
Resposta Correta: np.random.seed()

Pergunta: Qual comando une dois ou mais arrays lado a lado, formando colunas em uma matriz?  
A) np.column_stack()  
B) np.concatenate()  
C) np.vstack()  
D) np.hstack()  
Resposta Correta: np.column_stack()

Pergunta: Qual comando salva um array NumPy em um arquivo de texto, como um arquivo CSV?  
A) np.savetxt()  
B) np.save()  
C) np.export_csv()  
D) np.write()  
Resposta Correta: np.savetxt()

Pergunta: Qual comando salva o array dados em um arquivo CSV com vírgulas como delimitador?  
A) np.save('dados.csv', dados, delimiter=',')  
B) np.savetxt('dados.csv', dados, delimiter=',')  
C) np.savetxt(dados, 'dados.csv', sep=',')  
D) np.write_csv('dados.csv', dados, sep=',')  
Resposta Correta: np.savetxt('dados.csv', dados, delimiter=',')

